import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  root: {
    padding: "16px",
    paddingInline:"0px"
  },
  container:{
      marginTop:"4px"
  }
}));

export default useStyles;
