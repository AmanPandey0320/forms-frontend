const deleteSurveyById = (id) => {
  return new Promise((resolve, reject) => {
    try {
        const url = ``
    } catch (error) {
      reject({ error, status: false, message: "Error in deleting survey" });
      return;
    }
  });
};
