import styled from "styled-components";
import Col from "./col";
import Row from "./row";

const Grid = styled.div``;

const GridSystem = {
  row: Row,
  col: Col,
  grid: Grid,
};

export default GridSystem;
