import Grid from "../grid";

const Brand = (props) => {
  return (
    <>
      <Grid.grid>
        <Grid.row justifyContent="initial">
          <Grid.col size={1} justifyContent="initial">
          </Grid.col>
        </Grid.row>
      </Grid.grid>
    </>
  );
};

export default Brand;
